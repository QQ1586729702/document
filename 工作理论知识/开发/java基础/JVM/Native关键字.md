## Native：

**Native**就是Java语言**为了调用其他语言**而写的一个**关键字**；产生原因：在之前C和C++盛行的时候，Java对于底层的调用还是需要调用C和C++的代码。

## 几个专业名词：

**NMS**（native method stack）：本地方法栈，主要是来调用本地方法接口的方法。

**JNI**（java native interface）：NMS调用NMI

**NMI**(native method interface)：本地方法接口，在本地方法库具体实现。
![[clipboard (12).png]]


#java #jvm 